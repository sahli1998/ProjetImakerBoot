version: '3.7'

services:

  angular-frontend:
    container_name: imakerbotapp-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '80:80'
    environment:
      - NODE_ENV=production